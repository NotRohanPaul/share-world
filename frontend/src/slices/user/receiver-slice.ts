import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const reducers = {

};

export const receiverSlice = createSlice({
    name: "receiver",
    initialState,
    reducers
});