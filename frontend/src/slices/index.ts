import { userSlice } from "./auth/user-slice";
import { receiverSlice } from "./user/receiver-slice";
import { senderSlice } from "./user/sender-slice";

export {
    userSlice,
    senderSlice,
    receiverSlice,
};